
from pyglet.window import key

def init(game):
    game.data["interface_array"] = [0]*8


def main(game):
    interface_array=game.data["interface_array"]

    for x in range(1):
        interface_array



